import utils

class Genomics:
    check_if_clump = utils.check_if_clump,
    compute_frequencies = utils.compute_frequencies,
    count_non_overlapping_pattern_occurrences = utils.count_non_overlapping_pattern_occurrences,
    count_pattern_occurrences = utils.count_pattern_occurrences,
    get_nucleotide_by_index = utils.get_nucleotide_by_index,
    get_nucleotide_value = utils.get_nucleotide_value,
    number_to_pattern = utils.number_to_pattern,
    pattern_to_number = utils.pattern_to_number,
    reverse_complement_string = utils.reverse_complement_string